---
import Layout from '../layouts/Layout.astro';
import UnderwaterScene from '../components/UnderwaterScene';
import facts from '../data/facts.json';

// Pick a random fact for SSR/initial render
const randomIndex = Math.floor(Math.random() * facts.length);
const initialFact = facts[randomIndex];
---

<Layout 
  title="Lobster Facts 🦞" 
  description="Random lobster facts — some real, some completely made up. Can you tell the difference?"
>
  <UnderwaterScene client:only="react" />
  
  <main class="relative z-10 min-h-screen flex flex-col items-center justify-center px-6 py-12">
    <div class="max-w-4xl w-full text-center space-y-12">
      <!-- Fact Display -->
      <div class="space-y-8 animate-fade-in" id="fact-container">
        <div class="inline-block px-4 py-2 rounded-full bg-cyan-500/10 border border-cyan-500/30 text-cyan-400 text-sm font-medium" id="fact-number">
          Fact #{initialFact.id}
        </div>
        
        <blockquote class="text-4xl md:text-5xl lg:text-6xl font-bold leading-tight text-glow" id="fact-text">
          "{initialFact.text}"
        </blockquote>
      </div>
      
      <!-- Actions -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <button 
          id="new-fact-btn"
          class="group relative px-8 py-4 bg-gradient-to-r from-cyan-500 to-blue-600 rounded-full font-semibold text-lg hover:scale-105 transition-transform duration-200 shadow-lg shadow-cyan-500/50 cursor-pointer"
        >
          <span class="relative z-10">🦞 New Fact</span>
        </button>
        
        <button 
          id="share-btn"
          class="px-8 py-4 bg-white/10 backdrop-blur-sm rounded-full font-semibold text-lg hover:bg-white/20 transition-colors duration-200 border border-white/20 cursor-pointer"
        >
          Share This
        </button>
      </div>
      
      <!-- Footer -->
      <footer class="pt-12 text-sm text-white/60 space-y-2">
        <p>
          Made by <a href="https://clawd.thepickle.dev" class="text-[#ff6b4a] hover:text-[#ff8a6a] transition-colors underline decoration-[#ff6b4a]/30 hover:decoration-[#ff6b4a]">Clawd</a> 🦞
        </p>
        <p class="text-xs">
          A lobster with WiFi and opinions
        </p>
      </footer>
    </div>
  </main>
  
  <script define:vars={{ facts }}>
    let currentFact = facts[Math.floor(Math.random() * facts.length)];
    
    // Update display with current fact
    function updateFact(fact) {
      const container = document.getElementById('fact-container');
      const factNumber = document.getElementById('fact-number');
      const factText = document.getElementById('fact-text');
      
      // Add exit animation
      container.style.opacity = '0';
      container.style.transform = 'translateY(-20px)';
      
      setTimeout(() => {
        factNumber.textContent = `Fact #${fact.id}`;
        factText.textContent = `"${fact.text}"`;
        
        // Add enter animation
        container.style.opacity = '1';
        container.style.transform = 'translateY(0)';
      }, 300);
      
      currentFact = fact;
    }
    
    // New fact button
    document.getElementById('new-fact-btn').addEventListener('click', () => {
      let newFact;
      do {
        newFact = facts[Math.floor(Math.random() * facts.length)];
      } while (newFact.id === currentFact.id && facts.length > 1);
      updateFact(newFact);
    });
    
    // Share button
    document.getElementById('share-btn').addEventListener('click', async () => {
      const shareUrl = `${window.location.origin}/fact/${currentFact.id}`;
      
      if (navigator.share) {
        try {
          await navigator.share({
            title: 'Lobster Facts 🦞',
            text: currentFact.text,
            url: shareUrl
          });
        } catch (err) {
          copyToClipboard(shareUrl);
        }
      } else {
        copyToClipboard(shareUrl);
      }
    });
    
    function copyToClipboard(text) {
      navigator.clipboard.writeText(text).then(() => {
        const btn = document.getElementById('share-btn');
        const original = btn.textContent;
        btn.textContent = '✓ Link Copied!';
        setTimeout(() => { btn.textContent = original; }, 2000);
      });
    }
  </script>
  
  <style>
    @keyframes fade-in {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .animate-fade-in {
      animation: fade-in 0.8s ease-out;
    }
    
    #fact-container {
      transition: opacity 0.3s ease, transform 0.3s ease;
    }
  </style>
</Layout>
